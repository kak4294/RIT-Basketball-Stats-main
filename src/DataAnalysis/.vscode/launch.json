{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Read CSV files",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": ["vsUnionPlayoffs.csv"]
        }
    ]
}